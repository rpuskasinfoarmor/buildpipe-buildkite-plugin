projects:
 - label: package project
   path: 
     - package
 - label: deploy project
   skip: 
    - package
   path:
     - deploy
 - label: test project
   skip:
     - package
     - deploy
   path: 
      - test
steps:
  - label: package
    env:
      BUILDPIPE_SCOPE: distinct
    command:
      - make package
  - wait
  - label: deploy
    env:
      BUILDPIPE_SCOPE: distinct
    command:
      - make deploy
  - wait
  - label: test
    env:
      BUILDPIPE_SCOPE: distinct
    command:
      - make test
  